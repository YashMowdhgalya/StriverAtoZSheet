#include<iostream>
using namespace std;

int main()
{
    /* Integer hashing using integer array 
    int n;
    cin >> n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin >> arr[i];
    }

    //pre compute
    int hash[13];
    for(int i=0;i<n;i++)
    {
        hash[arr[i]]++;
    }
    
    //fetch
    int q, num;
    cin >> q;
    while(q--)
    {
        cin >> num;
        cout << hash[num]; 
    }

    */
   int n;
   cin >> n;
   char arr[n];
   for(int i=0;i<n;i++)
   {
    
   }
    
    
}